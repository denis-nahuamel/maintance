export interface IncidenteInterface {
		 fechaIncidencia: string; // Contiene la frase
  descripcion: string;
  laboratorio: string; // Identificador la frase aleatoria
  equipo: string; // URL que contiene el ID y permite recuperar la frase
}
